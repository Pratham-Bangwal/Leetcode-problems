#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n,no,flag;
    scanf("%d",&n);
    int list[n];
    for(int i=0;i<n;i++){
        scanf("%d",&list[i]);
    }
    scanf("%d",&no);
    for(int i=0;i<n;i++){
        if(list[i]==no){
            flag = 1;
            break;
        }else{
            flag = 0;
        }
    }
    if(flag==1){
        printf("Found");
    }else{
        printf("Not Found");
    }
    return 0;
}
